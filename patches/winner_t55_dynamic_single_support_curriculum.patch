diff --git a/playground/open_duck_mini_v2/joystick.py b/playground/open_duck_mini_v2/joystick.py
--- a/playground/open_duck_mini_v2/joystick.py
+++ b/playground/open_duck_mini_v2/joystick.py
@@ -29,6 +29,7 @@ from . import constants
 from . import base as open_duck_mini_v2_base
 from playground.common import winner_v3_variable_configuration as winner_v3
 from playground.common import t19_support_trainthrough as t19
+from playground.common import t55_dynamic_single_support_curriculum as t55
 
 # from playground.common.utils import LowPassActionFilter
 from playground.common.poly_reference_motion import PolyReferenceMotion
@@ -222,6 +223,8 @@ def default_config() -> config_dict.ConfigDict:
         winner_v119_train_transition_match=False,
         winner_t19_support_trainthrough=False,
         winner_t31_action_margin_trainthrough=False,
+        winner_t55_balance_stage=False,
+        winner_t55_transfer_stage=False,
     )
 
 
@@ -504,6 +507,10 @@ class Joystick(open_duck_mini_v2_base.OpenDuckMiniV2Env):
                 "cost/winner_v127_dense_torque_exceedance"
             ] = jp.zeros(())
+        if (
+            self._config.winner_t55_balance_stage
+            or self._config.winner_t55_transfer_stage
+        ):
+            metrics["reward/t55_single_support_balance"] = jp.zeros(())
+            metrics["reward/t55_left_support_match"] = jp.zeros(())
+            metrics["reward/t55_right_support_match"] = jp.zeros(())
 
         contact = jp.array(
             [
@@ -778,6 +785,23 @@ class Joystick(open_duck_mini_v2_base.OpenDuckMiniV2Env):
             k: v * self._config.reward_config.scales[k] for k, v in rewards.items()
         }
         reward = jp.clip(sum(rewards.values()) * self.dt, 0.0, 10000.0)
+        if (
+            self._config.winner_t55_balance_stage
+            or self._config.winner_t55_transfer_stage
+        ):
+            support_reward = t55.single_support_balance_reward(
+                self.get_gravity(data),
+                self.get_gyro(data),
+                contact,
+                state.info["imitation_phase"],
+            )
+            state.metrics["reward/t55_single_support_balance"] = (
+                support_reward
+            )
+            support_sides = t55.matched_support_sides(
+                contact,
+                state.info["imitation_phase"],
+            )
+            state.metrics["reward/t55_left_support_match"] = (
+                support_sides[0].astype(jp.float32)
+            )
+            state.metrics["reward/t55_right_support_match"] = (
+                support_sides[1].astype(jp.float32)
+            )
+            reward = t55.curriculum_reward(
+                reward,
+                support_reward,
+                balance_stage=self._config.winner_t55_balance_stage,
+                transfer_stage=self._config.winner_t55_transfer_stage,
+            )
         # jax.debug.print('STEP REWARD: {}',reward)
         state.info["push"] = push
         state.info["step"] += 1
diff --git a/playground/open_duck_mini_v2/runner.py b/playground/open_duck_mini_v2/runner.py
--- a/playground/open_duck_mini_v2/runner.py
+++ b/playground/open_duck_mini_v2/runner.py
@@ -103,6 +103,10 @@ class OpenDuckMiniV2Runner(BaseRunner):
         self.env_config.winner_t31_action_margin_trainthrough = (
             args.winner_t31_action_margin_trainthrough
         )
+        self.env_config.winner_t55_balance_stage = (
+            args.winner_t55_balance_stage
+        )
+        self.env_config.winner_t55_transfer_stage = (
+            args.winner_t55_transfer_stage
+        )
         if (
             args.winner_t31_action_margin_trainthrough
             and not args.winner_t19_support_trainthrough
@@ -112,6 +116,19 @@ class OpenDuckMiniV2Runner(BaseRunner):
                 "train-through"
             )
+        if (
+            args.winner_t55_balance_stage
+            and args.winner_t55_transfer_stage
+        ):
+            raise ValueError("T55 stages are mutually exclusive")
+        if (
+            args.winner_t55_balance_stage
+            or args.winner_t55_transfer_stage
+        ) and not (
+            args.winner_t19_support_trainthrough
+            and args.winner_t31_action_margin_trainthrough
+            and args.winner_v3_variable_configuration
+        ):
+            raise ValueError(
+                "T55 requires V3 plus T19/T31 train-through"
+            )
         self.env_config.winner_v127_constrained_cost = (
             args.winner_v127_constrained_cost
         )
@@ -383,6 +400,14 @@ def main() -> None:
         "--winner_t31_action_margin_trainthrough",
         action="store_true",
     )
+    parser.add_argument(
+        "--winner_t55_balance_stage",
+        action="store_true",
+    )
+    parser.add_argument(
+        "--winner_t55_transfer_stage",
+        action="store_true",
+    )
     parser.add_argument("--winner_v127_constrained_cost", action="store_true")
     parser.add_argument(
         "--winner_v3_deviation_scale",
