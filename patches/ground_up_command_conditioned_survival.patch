diff --git a/playground/open_duck_mini_v2/joystick.py b/playground/open_duck_mini_v2/joystick.py
--- a/playground/open_duck_mini_v2/joystick.py
+++ b/playground/open_duck_mini_v2/joystick.py
@@ -109,6 +109,7 @@ def default_config() -> config_dict.ConfigDict:
         nominal_reference_command_x=0.074,
         reference_start_phase=0,
         ground_up_signed_progress_objective=False,
+        ground_up_command_conditioned_survival=False,
     )
 
 
@@ -690,6 +691,13 @@ class Joystick(open_duck_mini_v2_base.OpenDuckMiniV2Env):
     ) -> dict[str, jax.Array]:
         del metrics  # Unused.
 
+        positive_forward_command = jp.abs(info["command"][0]) > 0.01
+        command_progress = jp.clip(
+            self.get_local_linvel(data)[0]
+            / jp.maximum(jp.abs(info["command"][0]), 0.01),
+            0.0,
+            1.0,
+        )
         ret = {
             "tracking_lin_vel": jp.where(
                 self._config.ground_up_signed_progress_objective
@@ -704,15 +712,30 @@ class Joystick(open_duck_mini_v2_base.OpenDuckMiniV2Env):
                     self._config.reward_config.tracking_sigma,
                 ),
             ),
-            "tracking_ang_vel": reward_tracking_ang_vel(
-                info["command"],
-                self.get_gyro(data),
-                self._config.reward_config.tracking_sigma,
+            "tracking_ang_vel": jp.where(
+                self._config.ground_up_command_conditioned_survival
+                & positive_forward_command,
+                command_progress
+                * reward_tracking_ang_vel(
+                    info["command"],
+                    self.get_gyro(data),
+                    self._config.reward_config.tracking_sigma,
+                ),
+                reward_tracking_ang_vel(
+                    info["command"],
+                    self.get_gyro(data),
+                    self._config.reward_config.tracking_sigma,
+                ),
             ),
             # "orientation": cost_orientation(self.get_gravity(data)),
             "torques": cost_torques(data.actuator_force),
             "action_rate": cost_action_rate(action, info["last_act"]),
-            "alive": reward_alive(),
+            "alive": jp.where(
+                self._config.ground_up_command_conditioned_survival
+                & positive_forward_command,
+                command_progress * reward_alive(),
+                reward_alive(),
+            ),
             "imitation": reward_imitation(  # FIXME, this reward is so adhoc...
                 self.get_floating_base_qpos(data.qpos),  # floating base qpos
                 self.get_local_linvel(data),  # reference velocity is body-local
diff --git a/playground/open_duck_mini_v2/runner.py b/playground/open_duck_mini_v2/runner.py
--- a/playground/open_duck_mini_v2/runner.py
+++ b/playground/open_duck_mini_v2/runner.py
@@ -33,6 +33,9 @@ class OpenDuckMiniV2Runner(BaseRunner):
         self.env_config.ground_up_signed_progress_objective = (
             args.ground_up_signed_progress_objective
         )
+        self.env_config.ground_up_command_conditioned_survival = (
+            args.ground_up_command_conditioned_survival
+        )
         if args.nominal_reference_bootstrap:
             self.env_config.noise_config.level = 0.0
             self.env_config.noise_config.action_min_delay = 0
@@ -87,6 +90,9 @@ def main() -> None:
     parser.add_argument("--nominal_reference_command_x", type=float, default=0.074)
     parser.add_argument("--reference_start_phase", type=int, default=0)
     parser.add_argument("--ground_up_signed_progress_objective", action="store_true")
+    parser.add_argument(
+        "--ground_up_command_conditioned_survival", action="store_true"
+    )
     parser.add_argument(
         "--critic_observation", choices=["privileged_state", "state"], default="privileged_state"
     )
