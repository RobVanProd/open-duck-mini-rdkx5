# Ground-Up Torso-COM Reset-Estimator Epsilon-Aware Wall Launcher Contract — 2026-07-15

## Result

`PASS_EPSILON_AWARE_WALL_LAUNCHER_CONTRACT`

All checks pass with zero Colab sessions, remote bytes, PPO steps, behavior
cells, or local accelerators.

The launcher freezes one fresh named T4 session, 24 exact hash-locked assets,
the original hosted job plus epsilon-aware wrapper, a 2,400-second total wall
ceiling, 120-second stop reserve, atomic manifest/archive recovery, and named
cleanup in `finally`. Compute usage is `UNMEASURED`; no rate, balance, or
operator-attestation surface exists. A mocked invalid execution fails closed
and still invokes successful cleanup. The dry plan cannot allocate without the
explicit allocation flag.

## Authority

Under the continuing user authorization already recorded in the
preregistration, this contract authorizes one single-use wall-only T4 launch
without another billing-number or approval prompt. There is no resume or retry.
Recovered artifacts remain behavior-unevaluated and authorize only the frozen
local CPU evaluation gate. No local GPU/iGPU, RDK-X5, runtime, or robot action
is authorized.

