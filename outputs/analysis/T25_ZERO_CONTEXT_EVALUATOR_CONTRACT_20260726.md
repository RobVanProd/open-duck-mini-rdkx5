# T25 diagnostic-zero context evaluator contract

- Status: `PASS_T25_ZERO_CONTEXT_EVALUATOR_CONTRACT`
- Both deployment graphs produced bit-exact outputs for zero versus nonzero diagnostic contexts over 256 recurrent ticks.
- The evaluator mode supplies an explicit `[1,64]` zero context without a calibrator or unscored prefix.
- Simulator behavior/Gate5/robot authority: `0/0/0`.
