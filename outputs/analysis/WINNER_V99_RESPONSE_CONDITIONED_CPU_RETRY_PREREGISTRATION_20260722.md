# Winner-v99 response-conditioned CPU retry preregistration

The Winner-v98 run stopped before reset, simulation, PPO, or optimization. Winner-v99 changes only the manual preflight cwd binding. All network sources, binaries, hyperparameters, populations, and thresholds remain frozen. Exactly one corrected CPU smoke is authorized; no hosted or robot work is authorized.
