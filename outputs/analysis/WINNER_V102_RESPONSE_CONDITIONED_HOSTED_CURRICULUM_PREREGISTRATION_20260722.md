# Winner-v102 response-conditioned hosted-curriculum preregistration

Winner-v101 passed its CPU mechanics contract. This freezes one GPU-only, no-retry 25% → 50% → 100% variable-configuration curriculum. The protected actor remains frozen; only the response adapter and critic may update. Build response is inferred automatically during reset, so no manual COM worksheet is required. This run creates training artifacts only: it does not evaluate behavior, select a deployment checkpoint, access the robot, or grant robot clearance.
