name: Winner v42 static target teacher table

on:
  push:
    branches:
      - codex/winner-v4-response-contract
    paths:
      - outputs/analysis/winner_v42_static_target_teacher_table_preregistration.json
      - outputs/analysis/WINNER_V42_STATIC_TARGET_TEACHER_TABLE_PREREGISTRATION_20260722.md

permissions:
  actions: read
  contents: read

concurrency:
  group: winner-v42-static-target-teacher-table
  cancel-in-progress: false

jobs:
  diagnostic:
    name: One CPU-only 15-configuration static-target teacher table
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - name: Reject workflow reruns
        run: test "${{ github.run_attempt }}" = "1"
      - name: Checkout frozen teacher-table contract
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up exact Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.13"
      - name: Install exact CPU environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            "jax[cpu]==0.7.2" "jaxlib==0.7.2" "mujoco==3.9.0" \
            "numpy==2.0.2" "onnx==1.22.0" "onnxruntime==1.27.0" "pytest==8.4.2"
      - name: Verify frozen teacher-table package
        run: |
          python -m pytest -q \
            tests/test_winner_v42_static_target_teacher_table.py \
            tests/test_winner_v42_static_target_teacher_table_preregistration.py \
            tests/test_winner_v42_static_target_teacher_table_import.py
      - name: Verify exact source manifest
        shell: bash
        run: |
          python - <<'PY'
          import hashlib, json
          from pathlib import Path
          value=json.loads(Path("outputs/analysis/winner_v42_static_target_teacher_table_preregistration.json").read_text())
          if value["status"] != "PREREGISTERED_WINNER_V42_STATIC_TARGET_TEACHER_TABLE" or any(value["execution_now"].values()):
              raise SystemExit("Winner-v42 authority changed")
          sources=value["sources"]
          for name,item in sources.items():
              observed=hashlib.sha256(Path(item["path"]).read_bytes().replace(b"\r\n",b"\n")).hexdigest()
              if item["hash_mode"] != "lf" or observed != item["sha256"]:
                  raise SystemExit(f"Winner-v42 source changed: {name}")
          canonical=hashlib.sha256(json.dumps(sources,sort_keys=True,separators=(",",":")).encode()).hexdigest()
          if canonical != value["source_manifest_sha256"]:
              raise SystemExit("Winner-v42 manifest changed")
          PY
      - name: Prepare exact reviewed simulation environment
        run: |
          git clone --quiet https://github.com/RobVanProd/Open_Duck_Playground.git /tmp/playground-source
          python tools/prepare_winner_v15_cpu_environment.py \
            --source-repository /tmp/playground-source \
            --output-root /tmp/pg115-v42-teacher-table \
            --work-root /tmp/winner-v42-teacher-table-environment \
            --fit-output /tmp/fixed_target_p30_actuator_fit_lf.json
      - name: Run exactly one CPU-only static-target teacher-table screen
        run: |
          python tools/run_winner_v42_static_target_teacher_table.py \
            --playground-root /tmp/pg115-v42-teacher-table \
            --canonical-fit /tmp/fixed_target_p30_actuator_fit_lf.json \
            --output /tmp/winner-v42-static-target-teacher-table-result.json \
            --offline-cpu-only --static-target-teacher-table-authorized
      - name: Hash teacher-table result
        if: always()
        run: |
          result=/tmp/winner-v42-static-target-teacher-table-result.json
          if test -f "$result"; then
            sha256sum "$result" | tee /tmp/winner-v42-static-target-teacher-table-result.sha256
          fi
      - name: Upload only teacher-table evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: winner-v42-static-target-teacher-table-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          compression-level: 6
          path: |
            /tmp/winner-v42-static-target-teacher-table-result.json
            /tmp/winner-v42-static-target-teacher-table-result.sha256
