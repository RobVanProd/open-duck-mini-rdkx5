name: Winner v23 negative-X response-use diagnostic

on:
  push:
    branches:
      - codex/winner-v4-response-contract
    paths:
      - outputs/analysis/winner_v23_negative_x_response_use_diagnostic_preregistration.json
      - outputs/analysis/WINNER_V23_NEGATIVE_X_RESPONSE_USE_DIAGNOSTIC_PREREGISTRATION_20260721.md

permissions:
  actions: read
  contents: read

concurrency:
  group: winner-v23-negative-x-response-use-diagnostic
  cancel-in-progress: false

jobs:
  diagnostic:
    name: One frozen read-only 20-pair CPU diagnostic
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Reject workflow reruns
        run: test "${{ github.run_attempt }}" = "1"

      - name: Checkout frozen diagnostic
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up exact Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.13"

      - name: Install exact CPU environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            "jax[cpu]==0.7.2" \
            "jaxlib==0.7.2" \
            "mujoco==3.9.0" \
            "numpy==2.0.2" \
            "onnx==1.22.0" \
            "onnxruntime==1.27.0" \
            "pytest==8.4.2"

      - name: Verify frozen diagnostic package
        run: |
          python -m pytest -q \
            tests/test_winner_v23_negative_x_response_use_diagnostic.py \
            tests/test_winner_v23_negative_x_response_use_diagnostic_preregistration.py \
            tests/test_winner_v23_negative_x_response_use_diagnostic_import.py

      - name: Resolve exact imported Winner-v22 training artifact
        id: training
        shell: bash
        run: |
          python - <<'PY' >>"$GITHUB_OUTPUT"
          import hashlib
          import json
          from pathlib import Path

          path = Path("outputs/analysis/winner_v23_negative_x_response_use_diagnostic_preregistration.json")
          value = json.loads(path.read_text())
          if value["status"] != "PREREGISTERED_WINNER_V23_NEGATIVE_X_RESPONSE_USE_DIAGNOSTIC":
              raise SystemExit("diagnostic is not preregistered")
          if value["execution_now"] != {
              "paired_cells": 0,
              "physics_rollouts": 0,
              "optimizer_updates": 0,
              "locomotion_steps": 0,
              "robot_or_rdk_access": 0,
          }:
              raise SystemExit("diagnostic authority changed")
          sources = value["sources"]
          for name, item in sources.items():
              observed = hashlib.sha256(
                  Path(item["path"]).read_bytes().replace(b"\r\n", b"\n")
              ).hexdigest()
              if item["hash_mode"] != "lf" or observed != item["sha256"]:
                  raise SystemExit(f"diagnostic source changed: {name}")
          canonical = hashlib.sha256(
              json.dumps(sources, sort_keys=True, separators=(",", ":")).encode()
          ).hexdigest()
          if canonical != value["source_manifest_sha256"]:
              raise SystemExit("diagnostic source manifest changed")
          attribution = value["training_artifact"]["repository_attribution"]
          if attribution["repository"] != "RobVanProd/open-duck-mini-rdkx5":
              raise SystemExit("training artifact repository changed")
          if attribution["github_run_attempt"] != 1:
              raise SystemExit("training source run was retried")
          print(f"run_id={attribution['github_run_id']}")
          print(f"artifact_name={attribution['github_artifact_name']}")
          PY

      - name: Download exact verified Winner-v22 training artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ steps.training.outputs.artifact_name }}
          path: /tmp/winner-v22-training-artifact
          repository: RobVanProd/open-duck-mini-rdkx5
          run-id: ${{ steps.training.outputs.run_id }}
          github-token: ${{ github.token }}

      - name: Verify extracted checkpoint identities
        run: |
          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          value = json.loads(
              Path("outputs/analysis/winner_v23_negative_x_response_use_diagnostic_preregistration.json").read_text()
          )
          root = Path(
              "/tmp/winner-v22-training-artifact/"
              "winner-v22-normalized-predictor-training-work"
          )
          for label, update in (("half", 50), ("final", 100)):
              expected = value["training_artifact"][label]
              paths = {
                  "snapshot": root / "snapshots" / f"snapshot_normalized_predictor_update_{update:03d}.npz",
                  "graph": root / "graphs" / f"winner_v22_{label}.onnx",
              }
              for kind, path in paths.items():
                  receipt = expected[kind]
                  observed = hashlib.sha256(path.read_bytes()).hexdigest()
                  if observed != receipt["sha256"] or path.stat().st_size != receipt["bytes"]:
                      raise SystemExit(f"{label} {kind} differs from imported artifact")
          PY

      - name: Prepare exact reviewed simulation environment
        run: |
          git clone --quiet https://github.com/RobVanProd/Open_Duck_Playground.git \
            /tmp/playground-source
          python tools/prepare_winner_v15_cpu_environment.py \
            --source-repository /tmp/playground-source \
            --output-root /tmp/pg115-v23-diagnostic \
            --work-root /tmp/winner-v23-diagnostic-environment \
            --fit-output /tmp/fixed_target_p30_actuator_fit_lf.json

      - name: Run exactly one frozen read-only negative-X diagnostic
        run: |
          python tools/run_winner_v23_negative_x_response_use_diagnostic.py \
            --training-work-root /tmp/winner-v22-training-artifact/winner-v22-normalized-predictor-training-work \
            --playground-root /tmp/pg115-v23-diagnostic \
            --canonical-fit /tmp/fixed_target_p30_actuator_fit_lf.json \
            --output /tmp/winner-v23-negative-x-response-use-diagnostic-result.json \
            --offline-cpu-only \
            --read-only-diagnostic-authorized

      - name: Hash diagnostic result
        if: always()
        run: |
          result=/tmp/winner-v23-negative-x-response-use-diagnostic-result.json
          if test -f "$result"; then
            sha256sum "$result" \
              | tee /tmp/winner-v23-negative-x-response-use-diagnostic-result.sha256
          fi

      - name: Upload only diagnostic evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: winner-v23-negative-x-response-use-diagnostic-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          compression-level: 6
          path: |
            /tmp/winner-v23-negative-x-response-use-diagnostic-result.json
            /tmp/winner-v23-negative-x-response-use-diagnostic-result.sha256
