name: Winner v45 static target teacher one update CPU

on:
  push:
    branches:
      - codex/winner-v4-response-contract
    paths:
      - outputs/analysis/winner_v45_static_target_teacher_one_update_cpu_contract.json
      - outputs/analysis/WINNER_V45_STATIC_TARGET_TEACHER_ONE_UPDATE_CPU_CONTRACT_20260722.md

permissions:
  actions: read
  contents: read

concurrency:
  group: winner-v45-static-target-teacher-one-update-cpu
  cancel-in-progress: false

jobs:
  proof:
    name: Exactly one CPU optimizer update
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Reject workflow reruns
        run: test "${{ github.run_attempt }}" = "1"
      - name: Checkout frozen one-update contract
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up exact Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.13"
      - name: Install exact CPU environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            "jax[cpu]==0.7.2" "jaxlib==0.7.2" "mujoco==3.9.0" \
            "numpy==2.0.2" "onnx==1.22.0" "onnxruntime==1.27.0" "pytest==8.4.2"
      - name: Verify frozen one-update package
        run: |
          python -m pytest -q \
            tests/test_winner_v43_static_target_teacher.py \
            tests/test_winner_v44_static_target_teacher_source_gradient_contract.py \
            tests/test_winner_v45_static_target_teacher_one_update_cpu_proof.py \
            tests/test_winner_v45_static_target_teacher_one_update_cpu_preregistration.py \
            tests/test_winner_v45_static_target_teacher_one_update_cpu_import.py
      - name: Resolve exact source artifacts and verify manifest
        id: inputs
        shell: bash
        run: |
          python - <<'PY' >>"$GITHUB_OUTPUT"
          import hashlib, json
          from pathlib import Path
          value=json.loads(Path("outputs/analysis/winner_v45_static_target_teacher_one_update_cpu_contract.json").read_text())
          if value["status"] != "PREREGISTERED_WINNER_V45_STATIC_TARGET_TEACHER_ONE_UPDATE_CPU_PROOF" or any(value["execution_now"].values()):
              raise SystemExit("Winner-v45 authority changed")
          sources=value["sources"]
          for name,item in sources.items():
              observed=hashlib.sha256(Path(item["path"]).read_bytes().replace(b"\r\n",b"\n")).hexdigest()
              if item["hash_mode"] != "lf" or observed != item["sha256"]:
                  raise SystemExit(f"Winner-v45 source changed: {name}")
          canonical=hashlib.sha256(json.dumps(sources,sort_keys=True,separators=(",",":")).encode()).hexdigest()
          if canonical != value["source_manifest_sha256"]:
              raise SystemExit("Winner-v45 manifest changed")
          v32=value["artifact_inputs"]["winner_v32"]["repository_attribution"]
          v22=value["artifact_inputs"]["winner_v22"]["repository_attribution"]
          for name,item in (("v32",v32),("v22",v22)):
              if item["repository"] != "RobVanProd/open-duck-mini-rdkx5" or item["github_run_attempt"] != 1:
                  raise SystemExit(f"Winner-v45 {name} attribution changed")
          print(f"v32_run_id={v32['github_run_id']}")
          print(f"v32_artifact_name={v32['github_artifact_name']}")
          print(f"v22_run_id={v22['github_run_id']}")
          print(f"v22_artifact_name={v22['github_artifact_name']}")
          PY
      - name: Download exact Winner-v32 source artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ steps.inputs.outputs.v32_artifact_name }}
          path: /tmp/winner-v32-source
          repository: RobVanProd/open-duck-mini-rdkx5
          run-id: ${{ steps.inputs.outputs.v32_run_id }}
          github-token: ${{ github.token }}
      - name: Download exact Winner-v22 teacher artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ steps.inputs.outputs.v22_artifact_name }}
          path: /tmp/winner-v22-teacher
          repository: RobVanProd/open-duck-mini-rdkx5
          run-id: ${{ steps.inputs.outputs.v22_run_id }}
          github-token: ${{ github.token }}
      - name: Prepare exact reviewed simulation environment
        run: |
          git clone --quiet https://github.com/RobVanProd/Open_Duck_Playground.git /tmp/playground-source
          python tools/prepare_winner_v15_cpu_environment.py \
            --source-repository /tmp/playground-source \
            --output-root /tmp/pg115-v45-one-update \
            --work-root /tmp/winner-v45-one-update-environment \
            --fit-output /tmp/fixed_target_p30_actuator_fit_lf.json
      - name: Run exactly one CPU optimizer update
        run: |
          python tools/run_winner_v45_static_target_teacher_one_update_cpu_proof.py \
            --playground-root /tmp/pg115-v45-one-update \
            --canonical-fit /tmp/fixed_target_p30_actuator_fit_lf.json \
            --v32-training-work-root /tmp/winner-v32-source/winner-v32-prefix-anchor-training-work \
            --v22-training-work-root /tmp/winner-v22-teacher/winner-v22-normalized-predictor-training-work \
            --work-root /tmp/winner-v45-static-target-teacher-one-update-work \
            --output /tmp/winner-v45-static-target-teacher-one-update-result.json \
            --offline-cpu-only --one-update-static-target-teacher-proof-authorized
      - name: Hash one-update result
        if: always()
        run: |
          result=/tmp/winner-v45-static-target-teacher-one-update-result.json
          if test -f "$result"; then
            sha256sum "$result" | tee /tmp/winner-v45-static-target-teacher-one-update-result.sha256
          fi
      - name: Upload only one-update evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: winner-v45-static-target-teacher-one-update-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          compression-level: 6
          path: |
            /tmp/winner-v45-static-target-teacher-one-update-result.json
            /tmp/winner-v45-static-target-teacher-one-update-result.sha256
            /tmp/winner-v45-static-target-teacher-one-update-work/winner_v45_static_target_teacher_update_252.npz
            /tmp/winner-v45-static-target-teacher-one-update-work/winner_v45_static_target_teacher_update_252.onnx
